# MyMac 项目规划

## 项目目标

开发一款 macOS 电脑管家软件，帮助用户更方便地管理自己的电脑。核心定位是「轻量、快速、原生」，通过状态栏快速观察系统状态，随时打开主窗口进行管理操作。

## 核心功能

1. **系统资源监控**：实时展示 CPU、内存、磁盘、网络等资源占用情况。
2. **应用快速卸载**：列出已安装应用（含图标、版本、占用空间），一键卸载到废纸篓。
3. **启动项管理**：查看登录启动项，支持启用 / 禁用，加快开机速度。
4. **状态栏入口**：菜单栏常驻图标，悬停/点击即可快速查看资源占用，并可快速打开主窗口。

## 技术选型

| 层次 | 技术 |
| ---- | ---- |
| 桌面框架 | Tauri 2.x |
| 后端语言 | Rust |
| 前端框架 | Vue 3 + TypeScript |
| 构建工具 | Vite |
| UI 组件 | shadcn-vue + Tailwind CSS |
| 包管理 | yarn |
| 资源采集 | sysinfo |
| 系统交互 | launchctl / NSWorkspace / 文件系统 |

## 架构说明

- **前端（Vue）**：负责界面展示与交互，通过 Tauri `invoke` 调用后端命令。
- **后端（Rust）**：通过 Tauri command 暴露系统能力，封装资源采集、应用扫描、启动项管理等逻辑。
- **状态栏（Tray）**：由 Rust 端创建菜单栏图标，动态更新资源占用文案，点击唤起主窗口。

## 版本里程碑

### v0.1（基础雏形）

- 搭建 Tauri 2 + Vue 3 项目骨架
- 实现系统资源监控（CPU / 内存 / 磁盘 / 网络）
- 实现应用列表展示与卸载
- 实现启动项列表与启用/禁用
- 实现状态栏菜单栏入口
- 基础 UI 框架（侧边栏导航 + 各功能页）

### v0.2（后续规划）

- 应用占用空间精确计算与缓存清理
- 进程管理（查看/结束进程）
- 网络流量明细
- 开机自启动与偏好设置
- 深色模式与主题定制

## 关键时间节点

- 基础雏形 v0.1：当前阶段
- 功能增强 v0.2：待 v0.1 稳定后推进
